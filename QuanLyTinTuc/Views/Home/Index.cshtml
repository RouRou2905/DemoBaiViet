@{
    ViewBag.Title = "Home Page";
}

<h1 style="text-align: center" class="p-5">TRANG CHỦ PHÓNG VIÊN</h1>

<!--

<!-- Content Header (Page header) -->
<!--<div class="content-header">
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12 col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="#">Quản lí tin tức</a></li>
                    <li class="breadcrumb-item">Danh sách tin tức</li>
                </ol>
            </div>
        </div>

        <div class="row">
            <div class="col-sm-6">
                <h1 class="ml-5">Danh sách tin tức</h1>
            </div>

            <div class="col-sm-6">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="out" href="~/QLTT/ThemTinTuc" role="button">
                            <i class="fas fa-pen-nib">Tạo tin</i>
                        </a>
                    </li>
                </ul>
            </div>
        </div>

    </div>-->
<!-- /.container-fluid -->
<!--</div>

<div class="container-fluid">
    <section class="content">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">-->
<!-- /.card-header -->
<!--<div class="card-body">
                <table id="example2" class="table table-hover ">
                    <thead class="bg-gradient-purple">
                        <tr>
                            <th class="">Danh mục</th>
                            <th class="">Tiêu đề tin</th>
                            <th class="">Mô tả</th>
                            <th class="">Ảnh đại diện</th>
                            <th class="">Chức năng</th>
                        </tr>
                    </thead>
                    <tbody id="tbl_data"></tbody>
                </table>
            </div>
        </div>
    </div>
</div>-->
@*/.card-body*@
@*/.card*@
    @*</section>
</div>*@

@*<script>
    function Delete_obj(id) {
        if (confirm("bạn có thực sự muốn xóa bản ghi có id =" + id + " này không?")) {
            //xử lý xóa
            /*alert("đồng ý xóa");*/

            //trường hợp không rỗng
            var formdata = new formdata();
            formdata.append("matin", id);

            $.ajax(
                {
                    type: "post",
                    data: formdata,
                    contenttype: "application/json; charset=utf-8",
                    url: window.location.origin + '/QLTT/Del_TT',
                    processdata: false,
                    contenttype: false,
                    cache: false,
                    success: function (response) {
                        var rs = json.parse(response);
                        alert(rs.errdesc);
                        //refresh lại trang web để thấy thay đổi;
                        window.location.href = "/QLTT/DSTT";
                    },
                    error: function (response) {
                        alert("xóa tin tức thất bại");
                    }
                }
            );
        }
    };

    $(document).ready(function () {
        $.ajax(
            {
                type: "POST",
                contentType: "application/json; charset=utf-8",
                url: window.location.origin + '/QLTT/get_All',
                processData: false,
                contentType: false,
                cache: false,
                success: function (response) {
                    var rs = JSON.parse(response);

                    if (rs.ErrCode == 1) {
                        //trường hợp lấy dssv thành công
                        console.log(rs.Data);
                        var str = "";
                        for (var i = 0; i < rs.Data.length; i++) {
                            str += "<tr> <td>" + rs.Data[i].category + "</td> <td>" + rs.Data[i].title + "</td> <td>" + rs.Data[i].script + "</td> <td>" + rs.Data[i].img + "</td> <td> <a href=' ./SuaTinTuc?matin=" + rs.Data[i].id + " ' class='btn btn-outline-warning border-0'> <i class='fas fa-edit'> </i></a> <button type='button' onclick = 'Delete_obj(" + rs.Data[i].id + ")' class = 'btn btn-danger'> <i class='fa fa-trash'></i> </button> </td> </tr>";
                        }
                        $("#tbl_data").html(str);
                    }
                    else {
                        alert(rs.ErrDesc);
                    }
                },
                error: function (response) {
                    alert("Lấy danh sách tin tức thất bại");
                }
            }
        );
    })

</script>-->*@